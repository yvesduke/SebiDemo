import Foundation
import ComposableArchitecture

// Define namespace for Foods feature module
extension DrSebi {
    enum Modules {
        enum Features {
            enum Foods {}
        }
    }
} 